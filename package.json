{
  "name": "tokenuse",
  "productName": "TokenUse",
  "version": "0.1.0",
  "private": true,
  "description": "实时监测 AI 编程工具的 token 消耗",
  "main": "dist/electron/main.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "server": "node dist/server/cli.js",
    "app": "electron .",
    "start": "npm run build && electron ."
  },
  "dependencies": {
    "echarts": "^6.1.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.0.0",
    "electron": "^44.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
